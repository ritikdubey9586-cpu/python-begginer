name = { 1,2,3,5,6,7,"ritik" , 8 , 4,4,3}

print(name)

# sets never print duplicate values and it is unordered collection of data type.

# sets is always written in curly brackets.

# if we want to write empty sets 

# info = {} # this will print dictionary because it is empty dictionary.
# print(type(info)) # this will print dictionary because it is empty dictionary.

info = set()
print(type(info)) # this will print set because it is an empty set.

# in sets we cant display any number as we can display in tuple 
# for example

print(name[0]) # this will give error because sets are unordered collection of data type.
